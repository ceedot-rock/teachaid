{
  "name": "teachaid",
  "version": "1.0.0",
  "private": true,
  "description": "TEACHAiD — SPLabs learning app",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:watch": "node --test --watch tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
