{
  "name": "teachaid",
  "version": "1.0.0",
  "private": true,
  "description": "TEACHAiD — SPLabs learning app",
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.test.js",
    "test:watch": "node --test --watch tests/*.test.js",
    "curriculum-audit": "node scripts/curriculum-audit.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "slid-phi": "^2.1.2"
  }
}
